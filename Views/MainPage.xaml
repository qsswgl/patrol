<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:PatrolApp.ViewModels"
             xmlns:models="clr-namespace:PatrolApp.Models"
             x:Class="PatrolApp.Views.MainPage"
             Title="巡更打卡"
             x:DataType="viewmodels:MainViewModel">

    <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="20">
        
        <!-- 当前状态显示 -->
        <Frame Grid.Row="0" 
               BorderColor="{StaticResource Primary}"
               CornerRadius="10"
               Padding="20"
               Margin="0,0,0,20">
            <VerticalStackLayout Spacing="10">
                <Label Text="当前位置"
                       FontSize="14"
                       TextColor="{StaticResource Gray600}"/>
                <Label Text="{Binding CurrentLocation}"
                       FontSize="24"
                       FontAttributes="Bold"
                       TextColor="{StaticResource Primary}"/>
                
                <Label Text="当前时间"
                       FontSize="14"
                       TextColor="{StaticResource Gray600}"
                       Margin="0,10,0,0"/>
                <Label Text="{Binding CurrentTime}"
                       FontSize="20"
                       FontAttributes="Bold"/>
                
                <HorizontalStackLayout Spacing="10" Margin="0,10,0,0">
                    <Label Text="NFC状态:"/>
                    <Label Text="{Binding IsNfcEnabled, Converter={StaticResource BoolToNfcStatusConverter}}"
                           TextColor="{Binding IsNfcEnabled, Converter={StaticResource BoolToColorConverter}}"/>
                </HorizontalStackLayout>
                
                <HorizontalStackLayout Spacing="10" IsVisible="{Binding LastNfcId, Converter={StaticResource StringToBoolConverter}}">
                    <Label Text="最后读卡:"/>
                    <Label Text="{Binding LastNfcId}"/>
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </Frame>

        <!-- 待同步记录提示 -->
        <Frame Grid.Row="1" 
               BorderColor="{StaticResource Warning}"
               CornerRadius="10"
               Padding="15"
               Margin="0,0,0,10"
               IsVisible="{Binding UnsyncedCount, Converter={StaticResource IntToBoolConverter}}">
            <HorizontalStackLayout Spacing="10">
                <Label Text="⚠️" FontSize="20"/>
                <Label Text="{Binding UnsyncedCount, StringFormat='有 {0} 条记录待同步'}"
                       VerticalOptions="Center"/>
                <Button Text="立即同步"
                        Command="{Binding SyncRecordsCommand}"
                        HorizontalOptions="EndAndExpand"
                        BackgroundColor="{StaticResource Warning}"
                        TextColor="White"
                        Padding="15,5"/>
            </HorizontalStackLayout>
        </Frame>

        <!-- 打卡记录列表 -->
        <Frame Grid.Row="2" 
               BorderColor="{StaticResource Gray200}"
               CornerRadius="10"
               Padding="0">
            <Grid RowDefinitions="Auto,*">
                <Label Grid.Row="0"
                       Text="打卡记录"
                       FontSize="18"
                       FontAttributes="Bold"
                       Padding="15,10"/>
                
                <CollectionView Grid.Row="1"
                                ItemsSource="{Binding Records}"
                                SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:PatrolRecord">
                            <Frame Margin="10,5"
                                   Padding="15"
                                   BorderColor="{StaticResource Gray200}"
                                   CornerRadius="8">
                                <Grid ColumnDefinitions="*,Auto">
                                    <VerticalStackLayout Grid.Column="0" Spacing="5">
                                        <Label Text="{Binding Location}"
                                               FontSize="16"
                                               FontAttributes="Bold"/>
                                        <Label Text="{Binding CheckInTime, StringFormat='{0:yyyy-MM-dd HH:mm:ss}'}"
                                               FontSize="14"
                                               TextColor="{StaticResource Gray600}"/>
                                        <Label Text="{Binding NfcId, StringFormat='NFC: {0}'}"
                                               FontSize="12"
                                               TextColor="{StaticResource Gray500}"/>
                                    </VerticalStackLayout>
                                    
                                    <Label Grid.Column="1"
                                           Text="{Binding IsSynced, Converter={StaticResource BoolToSyncStatusConverter}}"
                                           FontSize="12"
                                           VerticalOptions="Center"
                                           TextColor="{Binding IsSynced, Converter={StaticResource BoolToSyncColorConverter}}"/>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                    
                    <CollectionView.EmptyView>
                        <VerticalStackLayout VerticalOptions="Center"
                                           HorizontalOptions="Center"
                                           Padding="20">
                            <Label Text="📱"
                                   FontSize="48"
                                   HorizontalOptions="Center"/>
                            <Label Text="暂无打卡记录"
                                   FontSize="16"
                                   TextColor="{StaticResource Gray600}"
                                   HorizontalOptions="Center"
                                   Margin="0,10,0,0"/>
                            <Label Text="请将手机靠近NFC标签进行打卡"
                                   FontSize="14"
                                   TextColor="{StaticResource Gray500}"
                                   HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>
                </CollectionView>
            </Grid>
        </Frame>

        <!-- 底部说明 -->
        <Label Grid.Row="3"
               Text="提示: 支持息屏和锁屏后读卡,无需打开应用"
               FontSize="12"
               TextColor="{StaticResource Gray500}"
               HorizontalTextAlignment="Center"
               Margin="0,10,0,0"/>
    </Grid>

</ContentPage>
